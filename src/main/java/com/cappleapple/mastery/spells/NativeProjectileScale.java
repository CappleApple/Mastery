package com.cappleapple.mastery.spells;
/** Synced presentation scale on Iron's existing projectile entities. */
public interface NativeProjectileScale {
    float mastery$getScale();
    void mastery$setScale(float scale);
}
